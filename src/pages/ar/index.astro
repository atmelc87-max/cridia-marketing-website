---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { t } from '../../i18n/utils';

const lang = 'ar';
---

<Layout title="كريديا هاير - منصة توظيف بالذكاء الاصطناعي" lang={lang}>
  <Header lang={lang} />

  <!-- Hero Section -->
  <section class="bg-gradient-to-br from-blue-50 to-indigo-100 py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center max-w-4xl mx-auto">
        <h1 class="text-5xl md:text-6xl font-bold text-gray-900 mb-6">
          {t(lang, 'hero.headline')}
        </h1>
        <p class="text-xl text-gray-600 mb-8">
          {t(lang, 'hero.subheadline')}
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
          <a 
            href="https://hire.cridia.ai/register" 
            class="bg-blue-600 text-white px-8 py-4 rounded-lg hover:bg-blue-700 transition font-semibold text-lg shadow-lg"
          >
            {t(lang, 'hero.cta')}
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Problem Section -->
  <section class="py-20 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-4xl font-bold text-center text-gray-900 mb-12">
        {t(lang, 'problem.headline')}
      </h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="text-center p-6">
          <div class="text-5xl mb-4">⏰</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            {t(lang, 'problem.timeDrain')}
          </h3>
          <p class="text-gray-600">
            {t(lang, 'problem.timeDrainDesc')}
          </p>
        </div>
        <div class="text-center p-6">
          <div class="text-5xl mb-4">💸</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            {t(lang, 'problem.costlyMistakes')}
          </h3>
          <p class="text-gray-600">
            {t(lang, 'problem.costlyMistakesDesc')}
          </p>
        </div>
        <div class="text-center p-6">
          <div class="text-5xl mb-4">📉</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            {t(lang, 'problem.bias')}
          </h3>
          <p class="text-gray-600">
            {t(lang, 'problem.biasDesc')}
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Solution Section -->
  <section id="how-it-works" class="py-20 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-4xl font-bold text-center text-gray-900 mb-12">
        {t(lang, 'solution.headline')}
      </h2>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        <div class="bg-white p-6 rounded-lg shadow-md">
          <div class="text-blue-600 font-bold text-lg mb-2">1</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            {t(lang, 'solution.step1Title')}
          </h3>
          <p class="text-gray-600">
            {t(lang, 'solution.step1Desc')}
          </p>
        </div>
        <div class="bg-white p-6 rounded-lg shadow-md">
          <div class="text-blue-600 font-bold text-lg mb-2">2</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            {t(lang, 'solution.step2Title')}
          </h3>
          <p class="text-gray-600">
            {t(lang, 'solution.step2Desc')}
          </p>
        </div>
        <div class="bg-white p-6 rounded-lg shadow-md">
          <div class="text-blue-600 font-bold text-lg mb-2">3</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            {t(lang, 'solution.step3Title')}
          </h3>
          <p class="text-gray-600">
            {t(lang, 'solution.step3Desc')}
          </p>
        </div>
        <div class="bg-white p-6 rounded-lg shadow-md">
          <div class="text-blue-600 font-bold text-lg mb-2">4</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            {t(lang, 'solution.step4Title')}
          </h3>
          <p class="text-gray-600">
            {t(lang, 'solution.step4Desc')}
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section id="features" class="py-20 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-4xl font-bold text-center text-gray-900 mb-12">
        {t(lang, 'features.headline')}
      </h2>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        <div class="p-6 border rounded-lg hover:shadow-lg transition">
          <div class="text-3xl mb-3">🎯</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            {t(lang, 'features.precisionTitle')}
          </h3>
          <p class="text-gray-600">
            {t(lang, 'features.precisionDesc')}
          </p>
        </div>
        <div class="p-6 border rounded-lg hover:shadow-lg transition">
          <div class="text-3xl mb-3">🌐</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            {t(lang, 'features.bilingualTitle')}
          </h3>
          <p class="text-gray-600">
            {t(lang, 'features.bilingualDesc')}
          </p>
        </div>
        <div class="p-6 border rounded-lg hover:shadow-lg transition">
          <div class="text-3xl mb-3">🎤</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            {t(lang, 'features.voiceTitle')}
          </h3>
          <p class="text-gray-600">
            {t(lang, 'features.voiceDesc')}
          </p>
        </div>
        <div class="p-6 border rounded-lg hover:shadow-lg transition">
          <div class="text-3xl mb-3">🔒</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            {t(lang, 'features.teamTitle')}
          </h3>
          <p class="text-gray-600">
            {t(lang, 'features.teamDesc')}
          </p>
        </div>
        <div class="p-6 border rounded-lg hover:shadow-lg transition">
          <div class="text-3xl mb-3">📊</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            {t(lang, 'features.structuredTitle')}
          </h3>
          <p class="text-gray-600">
            {t(lang, 'features.structuredDesc')}
          </p>
        </div>
        <div class="p-6 border rounded-lg hover:shadow-lg transition">
          <div class="text-3xl mb-3">☁️</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            {t(lang, 'features.cloudTitle')}
          </h3>
          <p class="text-gray-600">
            {t(lang, 'features.cloudDesc')}
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Pricing Preview -->
  <section class="py-20 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-4xl font-bold text-center text-gray-900 mb-4">
        {t(lang, 'pricing.headline')}
      </h2>
      <p class="text-center text-gray-600 mb-12">
        {t(lang, 'pricing.trialText')}
      </p>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto">
        <div class="bg-white p-8 rounded-lg shadow-md border-2 border-gray-200">
          <h3 class="text-2xl font-bold text-gray-900 mb-2">
            {t(lang, 'pricing.starter')}
          </h3>
          <div class="mb-4">
            <span class="text-4xl font-bold text-blue-600">$99</span>
            <span class="text-lg text-gray-600">{t(lang, 'pricing.perMonth')}</span>
          </div>
          <ul class="space-y-3 mb-6">
            <li class="flex items-center text-gray-600">
              <span class="text-green-500 mr-2">✓</span>
              100 {t(lang, 'pricing.cvsPerMonth')}
            </li>
            <li class="flex items-center text-gray-600">
              <span class="text-green-500 mr-2">✓</span>
              40 {t(lang, 'pricing.interviews')}
            </li>
            <li class="flex items-center text-gray-600">
              <span class="text-green-500 mr-2">✓</span>
              3 {t(lang, 'pricing.teamMembers')}
            </li>
          </ul>
          <a 
            href="https://hire.cridia.ai/register" 
            class="block text-center bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition font-medium"
          >
            {t(lang, 'pricing.startTrial')}
          </a>
        </div>
        <div class="bg-white p-8 rounded-lg shadow-xl border-2 border-blue-600 relative">
          <div class="absolute -top-4 left-1/2 transform -translate-x-1/2 bg-blue-600 text-white px-4 py-1 rounded-full text-sm font-semibold">
            الأكثر شعبية
          </div>
          <h3 class="text-2xl font-bold text-gray-900 mb-2">
            {t(lang, 'pricing.professional')}
          </h3>
          <div class="mb-4">
            <span class="text-4xl font-bold text-blue-600">$249</span>
            <span class="text-lg text-gray-600">{t(lang, 'pricing.perMonth')}</span>
          </div>
          <ul class="space-y-3 mb-6">
            <li class="flex items-center text-gray-600">
              <span class="text-green-500 mr-2">✓</span>
              500 {t(lang, 'pricing.cvsPerMonth')}
            </li>
            <li class="flex items-center text-gray-600">
              <span class="text-green-500 mr-2">✓</span>
              100 {t(lang, 'pricing.interviews')}
            </li>
            <li class="flex items-center text-gray-600">
              <span class="text-green-500 mr-2">✓</span>
              10 {t(lang, 'pricing.teamMembers')}
            </li>
          </ul>
          <a 
            href="https://hire.cridia.ai/register" 
            class="block text-center bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition font-medium"
          >
            {t(lang, 'pricing.startTrial')}
          </a>
        </div>
        <div class="bg-white p-8 rounded-lg shadow-md border-2 border-gray-200">
          <h3 class="text-2xl font-bold text-gray-900 mb-2">
            {t(lang, 'pricing.enterprise')}
          </h3>
          <div class="mb-4">
            <span class="text-4xl font-bold text-blue-600">$699</span>
            <span class="text-lg text-gray-600">{t(lang, 'pricing.perMonth')}</span>
          </div>
          <ul class="space-y-3 mb-6">
            <li class="flex items-center text-gray-600">
              <span class="text-green-500 mr-2">✓</span>
              {t(lang, 'pricing.unlimited')} CVs
            </li>
            <li class="flex items-center text-gray-600">
              <span class="text-green-500 mr-2">✓</span>
              {t(lang, 'pricing.unlimited')} {t(lang, 'pricing.interviews')}
            </li>
            <li class="flex items-center text-gray-600">
              <span class="text-green-500 mr-2">✓</span>
              {t(lang, 'pricing.unlimited')} فريق
            </li>
          </ul>
          <a 
            href="https://hire.cridia.ai/register" 
            class="block text-center bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition font-medium"
          >
            {t(lang, 'pricing.startTrial')}
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section id="contact" class="py-20 bg-white">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-4xl font-bold text-gray-900 mb-4">
          {t(lang, 'contact.headline')}
        </h2>
        <p class="text-xl text-gray-600">
          {t(lang, 'contact.subheadline')}
        </p>
      </div>
      
      <form class="space-y-6">
        <div>
          <input 
            type="text" 
            placeholder={t(lang, 'contact.namePlaceholder')}
            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-600"
            required
          />
        </div>
        <div>
          <input 
            type="email" 
            placeholder={t(lang, 'contact.emailPlaceholder')}
            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-600"
            required
          />
        </div>
        <div>
          <textarea 
            placeholder={t(lang, 'contact.messagePlaceholder')}
            rows="6"
            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-600"
            required
          ></textarea>
        </div>
        <div class="text-center">
          <button 
            type="submit"
            class="bg-blue-600 text-white px-8 py-3 rounded-lg hover:bg-blue-700 transition font-medium"
          >
            {t(lang, 'contact.send')}
          </button>
        </div>
      </form>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="py-20 bg-gradient-to-br from-blue-600 to-indigo-700 text-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-4xl font-bold mb-4">
        {t(lang, 'finalCta.headline')}
      </h2>
      <p class="text-xl mb-8 text-blue-100">
        {t(lang, 'finalCta.subheadline')}
      </p>
      <a 
        href="https://hire.cridia.ai/register" 
        class="inline-block bg-white text-blue-600 px-8 py-4 rounded-lg hover:bg-gray-100 transition font-semibold text-lg shadow-lg"
      >
        {t(lang, 'finalCta.cta')}
      </a>
      
      <div class="mt-12 flex flex-wrap justify-center gap-8 text-sm text-blue-100">
        <div class="flex items-center gap-2">
          <span>🔒</span>
          {t(lang, 'finalCta.encryption')}
        </div>
        <div class="flex items-center gap-2">
          <span>🌐</span>
          {t(lang, 'finalCta.gdpr')}
        </div>
        <div class="flex items-center gap-2">
          <span>💳</span>
          {t(lang, 'finalCta.secure')}
        </div>
      </div>
    </div>
  </section>

  <Footer lang={lang} />
</Layout>